import React from 'react';
import useTitle from '../../myhooks/useTitle';

const MyReview = () => {
    useTitle('My Reviews')
    return (
        <div>
            
        </div>
    );
};

export default MyReview;